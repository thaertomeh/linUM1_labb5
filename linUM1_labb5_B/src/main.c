#include <stdio.h>
#include <stdlib.h>
#include "my_add.h"
int main ()
{
	   int res,x,y;
	   res=add(x,y);
	   printf("Add the two numbers are:%d\n",res);
	   printf( "Add new text line");
	   
	   return(0);
}


