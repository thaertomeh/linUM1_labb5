#include <stdio.h>
#include "my_add.h"
int add(int x,int y)
{
	     printf("Enter the first value number:");
	     scanf("%d",&x);
	     printf("Enter the next value number:");
	     scanf("%d",&y);
             return(x+y);
}
